spring:
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:32768/test_schema
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  redis:
    host:
    port: